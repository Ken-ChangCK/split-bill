# 分帳系統開發任務清單

## 專案資訊

- **專案名稱**: 簡易分帳系統 (Split Bill App)
- **技術棧**: React + Vite + Tailwind CSS + shadcn/ui
- **專案路徑**: `/Users/zhangzhekai/Documents/GitHub/split-bill`

---

## 階段一：專案初始化

### 任務 1.1: 建立 React + Vite 專案

- [ ] 在指定路徑建立專案資料夾
- [ ] 初始化 Vite + React 專案
- [ ] 安裝必要依賴：
  - `vite`
  - `@vitejs/plugin-react`
  - `react`
  - `react-dom`

### 任務 1.2: 設定 Tailwind CSS

- [ ] 安裝 Tailwind CSS 相關套件
  - `tailwindcss`
  - `postcss`
  - `autoprefixer`
- [ ] 建立 `tailwind.config.js`
- [ ] 建立 `postcss.config.js`
- [ ] 在 CSS 中引入 Tailwind directives

### 任務 1.3: 整合 shadcn/ui

- [ ] 安裝 shadcn/ui CLI
- [ ] 初始化 shadcn/ui 設定
- [ ] 建立 `components.json` 配置檔
- [ ] 設定 path aliases (@/components)

### 任務 1.4: 安裝所需的 shadcn/ui 元件

- [ ] Tabs (分頁功能)
- [ ] Button (按鈕)
- [ ] Input (輸入框)
- [ ] Select (下拉選單)
- [ ] Card (卡片容器)
- [ ] Badge (標籤)
- [ ] Alert (提示訊息)

---

## 階段二：核心功能開發

### 任務 2.1: 成員管理功能

**頁面**: 成員管理 Tab (👥 成員管理)

**功能需求**:

- [ ] 新增成員輸入框 + 按鈕
- [ ] 成員列表顯示
- [ ] 刪除成員功能 (帶確認提示)
- [ ] 成員資料儲存在 state 中
- [ ] 成員名稱不可重複驗證

**UI 元件**:

- Input (輸入成員名稱)
- Button (新增成員、刪除成員)
- Badge or Card (顯示成員列表)

**資料結構**:

```javascript
members: string[]  // ['小明', '小華', '小美']
```

---

### 任務 2.2: 新增支出功能

**頁面**: 新增支出 Tab (➕ 新增支出)

**功能需求**:

- [ ] 項目名稱輸入框
- [ ] 金額輸入框 (數字類型)
- [ ] 付款人下拉選單 (單選，從成員列表動態生成)
- [ ] 參與分攤者多選下拉選單 (可多選，從成員列表動態生成)
- [ ] 表單驗證：
  - 所有欄位必填
  - 金額必須 > 0
  - 至少選擇一個參與者
- [ ] 新增成功後：
  - 清空表單
  - 自動切換到「支出記錄」Tab

**UI 元件**:

- Input (項目名稱、金額)
- Select (付款人、參與者)
- Button (新增支出)

**資料結構**:

```javascript
expenses: [
  {
    id: timestamp,
    itemName: string,      // '晚餐'
    amount: number,        // 300
    payer: string,         // '小明'
    participants: string[] // ['小明', '小華', '小美']
  }
]
```

---

### 任務 2.3: 支出記錄列表

**頁面**: 支出記錄 Tab (📋 支出記錄)

**功能需求**:

- [ ] 顯示所有支出記錄
- [ ] 每筆記錄顯示：
  - 項目名稱
  - 金額 (醒目顯示)
  - 付款人
  - 參與分攤的人
  - 刪除按鈕
- [ ] 刪除功能 (帶確認提示)
- [ ] 空狀態提示：「尚無支出記錄」

**UI 元件**:

- Card (每筆支出)
- Button (刪除)
- Alert (空狀態)

---

### 任務 2.4: 結算計算功能

**頁面**: 結算結果 Tab (🧮 結算結果)

**功能需求**:

- [ ] 「計算欠款」按鈕
- [ ] 計算邏輯：
  1. 計算每個人的淨收支 (實際付款 - 應付金額)
  2. 顯示每個人的收支狀況 (應收/應付/已結清)
  3. 使用貪婪算法優化還款方案
- [ ] 結果顯示分兩部分：
  - **各人收支狀況**: 顯示每人應收/應付金額
  - **還款方案**: 顯示最少交易次數的還款路徑

**計算邏輯**:

```javascript
// 1. 計算每個人的淨額
balances[person] = 實際支付總額 - 應付總額

// 2. 分離債權人和債務人
creditors = balances 中 > 0 的人 (應收款)
debtors = balances 中 < 0 的人 (應付款)

// 3. 配對還款
while (還有債權人 && 還有債務人) {
  找最大債權人和最大債務人
  轉帳金額 = min(債權, 債務)
  記錄: "債務人 還給 債權人 $金額"
  更新雙方餘額
}
```

**UI 元件**:

- Button (計算欠款)
- Card (收支狀況列表)
- Card (還款方案列表)
- Badge (標示應收/應付)

---

## 階段三：UI/UX 優化

### 任務 3.1: Tab 切換功能

- [ ] 使用 shadcn/ui Tabs 元件
- [ ] 4 個分頁：
  1. 👥 成員管理
  2. ➕ 新增支出
  3. 📋 支出記錄
  4. 🧮 結算結果
- [ ] Tab 切換流暢動畫
- [ ] 新增支出後自動切換到支出記錄

### 任務 3.2: 樣式設計

- [ ] 使用漸層背景 (紫色系)
- [ ] 卡片式設計，圓角陰影
- [ ] 統一色彩方案：
  - 主色：紫色 (#667eea)
  - 應收款：綠色
  - 應付款：紅色
- [ ] 響應式設計 (手機/平板/桌面)
- [ ] hover 效果和動畫
- [ ] 空狀態友善提示

### 任務 3.3: 互動優化

- [ ] 表單驗證錯誤提示 (使用 Alert)
- [ ] 刪除確認對話框
- [ ] 操作成功提示
- [ ] Loading 狀態 (如需要)
- [ ] 多選下拉選單使用提示：「按住 Ctrl/Cmd 可多選」

---

## 階段四：進階功能 (可選)

### 任務 4.1: 資料持久化

- [ ] 使用 localStorage 儲存資料
- [ ] 頁面重新載入後恢復資料
- [ ] 清除所有資料功能

### 任務 4.2: 匯出功能

- [ ] 匯出結算結果為文字
- [ ] 匯出為 CSV 檔案
- [ ] 複製到剪貼簿功能

### 任務 4.3: 歷史記錄

- [ ] 記錄已完成的結算
- [ ] 查看歷史分帳記錄
- [ ] 重新開始新的分帳

---

## 專案結構建議

```
split-bill/
├── public/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui 元件
│   │   │   ├── tabs.tsx
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── select.tsx
│   │   │   ├── card.tsx
│   │   │   └── ...
│   │   ├── MemberManagement.tsx    # 成員管理元件
│   │   ├── ExpenseForm.tsx         # 新增支出表單
│   │   ├── ExpenseList.tsx         # 支出記錄列表
│   │   └── SettlementResult.tsx    # 結算結果
│   ├── lib/
│   │   └── utils.ts        # 工具函數
│   ├── hooks/              # 自定義 hooks (可選)
│   ├── App.tsx             # 主應用程式
│   ├── main.tsx
│   └── index.css
├── tailwind.config.js
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## 關鍵技術細節

### 成員下拉選單動態更新

當成員列表變更時，付款人和參與者的下拉選單需要同步更新。

### 多選下拉選單

使用 shadcn/ui 的 Select 元件的 multiple 模式，或使用 Checkbox Group。

### 結算算法

使用貪婪算法優化還款路徑，減少交易次數。

---

## 測試場景

### 測試案例 1: 三人平分晚餐

- 成員：小明、小華、小美
- 支出：小明付 300 元，三人平分
- 預期結果：
  - 小華欠小明 100
  - 小美欠小明 100

### 測試案例 2: 複雜分帳

- 成員：A、B、C
- 支出 1：A 付 300，ABC 平分 (每人應付 100)
- 支出 2：B 付 150，AB 平分 (每人應付 75)
- 支出 3：C 付 90，AC 平分 (每人應付 45)
- 預期結果：計算各人淨額並給出最優還款方案

---

## 開發建議

1. **先完成基本功能，再優化 UI**
2. **使用 TypeScript 以獲得更好的型別安全**
3. **元件化設計，保持程式碼可維護性**
4. **適當使用 React hooks (useState, useEffect)**
5. **shadcn/ui 元件按需安裝，不要全部安裝**

---

## 執行指令

### 初始化專案

```bash
cd /Users/zhangzhekai/Documents/GitHub/split-bill
npm create vite@latest . -- --template react-ts
npm install
```

### 安裝 Tailwind CSS

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### 安裝 shadcn/ui

```bash
npx shadcn-ui@latest init
npx shadcn-ui@latest add tabs button input select card badge alert
```

### 啟動開發伺服器

```bash
npm run dev
```

---

## 完成檢查清單

- [ ] 所有功能正常運作
- [ ] UI 美觀且響應式
- [ ] 無 console 錯誤
- [ ] 程式碼格式整齊
- [ ] 元件化良好
- [ ] 註解清楚
- [ ] README.md 完整

---

**最後更新**: 2025-11-12
**建立者**: Claude (根據對話整理)
