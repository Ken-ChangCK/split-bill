# GitHub Pages 部署指南

## 快速部署步驟

### 1. 推送程式碼到 GitHub

```bash
# 添加所有檔案
git add .

# 提交變更
git commit -m "完成分帳系統並配置 GitHub Pages 部署"

# 推送到 GitHub (如果還沒設定遠端倉庫)
git remote add origin https://github.com/你的使用者名稱/split-bill.git
git push -u origin main
```

### 2. 啟用 GitHub Pages

1. 前往你的 GitHub 儲存庫頁面
2. 點擊 **Settings** (設定)
3. 在左側選單找到 **Pages**
4. 在 **Source** 下拉選單中選擇 **GitHub Actions**
5. 儲存設定

### 3. 自動部署

當你推送程式碼到 `main` 分支時,GitHub Actions 會自動:
- 安裝依賴
- 執行建置
- 部署到 GitHub Pages

你可以在 **Actions** 標籤查看部署進度。

### 4. 訪問網站

部署完成後,你的網站會在以下網址可用:
```
https://你的使用者名稱.github.io/split-bill/
```

## 已完成的配置

### ✅ Vite 配置
- `vite.config.ts` 已設定 `base: '/split-bill/'`
- 這確保所有資源路徑在 GitHub Pages 上正確

### ✅ GitHub Actions Workflow
- `.github/workflows/deploy.yml` 已建立
- 自動在推送到 `main` 時觸發
- 使用官方 GitHub Pages 部署 action

### ✅ 建置測試
- 生產版本建置成功
- 所有依賴已安裝
- TypeScript 編譯無錯誤

## 疑難排解

### 問題: 頁面顯示 404

**解決方案:**
1. 確認 GitHub Pages 已在儲存庫設定中啟用
2. 確認 Source 設定為 "GitHub Actions"
3. 檢查 Actions 標籤確認部署成功

### 問題: 樣式沒有載入

**解決方案:**
- 確認 `vite.config.ts` 中的 `base` 路徑正確
- 應該是 `base: '/split-bill/'` (與儲存庫名稱一致)

### 問題: 部署失敗

**解決方案:**
1. 檢查 Actions 標籤的錯誤訊息
2. 確認所有檔案都已推送
3. 確認 `package.json` 中的所有依賴都正確

## 本地測試生產版本

在推送前,可以本地測試生產版本:

```bash
# 建置
npm run build

# 預覽
npm run preview
```

訪問顯示的網址 (通常是 http://localhost:4173/split-bill/)

## 更新部署

每次想要更新網站時:

```bash
# 修改程式碼後
git add .
git commit -m "更新說明"
git push origin main
```

GitHub Actions 會自動重新部署!

## 自訂網域 (選擇性)

如果你有自己的網域:

1. 在儲存庫根目錄建立 `public/CNAME` 檔案
2. 內容填入你的網域名稱
3. 在你的網域提供商設定 DNS 記錄

---

**部署完成!** 🎉

現在你的分帳系統可以在網路上公開訪問了!
